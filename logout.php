<?php
// فتح وسم PHP لبدء كتابة الكود

require_once 'includes/functions.php';
// استدعاء ملف الدوال المساعدة (functions.php) مرة واحدة فقط
// ملاحظة: هذا الملف عادةً يحتوي على session_start() ضمنياً لبدء الجلسة

// حذف جميع بيانات الجلسة
session_unset();
// حذف جميع المتغيرات المخزنة في الجلسة الحالية ($_SESSION)
// مثال: user_id, name, role التي خزّناها عند تسجيل الدخول

session_destroy();
// تدمير الجلسة بالكامل من الخادم
// (يحذف ملف الجلسة من السيرفر وينهي الجلسة نهائياً)

// العودة لصفحة الدخول
header('Location: login.php');
// إرسال ترويسة HTTP لتوجيه المتصفح إلى صفحة تسجيل الدخول (login.php)

exit;
// إيقاف تنفيذ السكربت فوراً
// مهم جداً بعد header() لضمان عدم تنفيذ أي كود آخر بعده